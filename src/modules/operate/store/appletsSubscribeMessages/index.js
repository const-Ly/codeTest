// 此文件也相当于中转
import templateLibrary from './templateLibrary'
import authorization from './authorization'

export default {
    modules: {
        templateLibrary,
        authorization
    },
}